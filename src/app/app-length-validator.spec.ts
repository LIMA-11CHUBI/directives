import { AppLengthValidator } from './app-length-validator';

describe('AppLengthValidator', () => {
  it('should create an instance', () => {
    const directive = new AppLengthValidator();
    expect(directive).toBeTruthy();
  });
});
