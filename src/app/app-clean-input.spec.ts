import { AppCleanInput } from './app-clean-input';

describe('AppCleanInput', () => {
  it('should create an instance', () => {
    const directive = new AppCleanInput();
    expect(directive).toBeTruthy();
  });
});
